<%= form_for @comment do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.collection_select :user_id, @users, :id, :username %>
  <%= f.fields_for :user_id do |user| %>
    <%= user.text_field :username %>
  <% end %>
  <%= f.submit %>
<% end %>
